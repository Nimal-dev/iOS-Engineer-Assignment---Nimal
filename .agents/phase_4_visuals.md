# Phase 4: On-Camera AR Visuals
- Configured `renderer(nodeFor anchor:)` in `ScannerViewModel`.
- Created a 3D bounding box (green outline) and a 2D-billboarded Tick mark (`checkmark.circle.fill`) that is attached to the physical anchor and always faces the user.
