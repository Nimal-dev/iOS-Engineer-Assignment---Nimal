# Phase 2: Core AR & Vision Integration
- Created `ARCameraView.swift` to handle the ARSCNView inside SwiftUI.
- Created `ProductDetectionService.swift` using `VNDetectRectanglesRequest` to simulate product object detection from the live AR feed.
